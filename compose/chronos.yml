services:
  chronos-template: &chronos-template
    extends:
      file: base.yml
      service: gpu-model-base
    image: chronos
    build:
      context: ../model-services/chronos
      dockerfile: Dockerfile
    profiles: ["chronos", "all-models"]

  chronos-bolt-tiny:
    <<: *chronos-template
    environment:
      - MODEL_ID=amazon/chronos-bolt-tiny

  chronos-bolt-small:
    <<: *chronos-template
    environment:
      - MODEL_ID=amazon/chronos-bolt-small

  chronos-bolt-base:
    <<: *chronos-template
    environment:
      - MODEL_ID=amazon/chronos-bolt-base

  chronos-bolt-mini:
    <<: *chronos-template
    environment:
      - MODEL_ID=amazon/chronos-bolt-mini

  chronos-2:
    <<: *chronos-template
    environment:
      - MODEL_ID=amazon/chronos-2