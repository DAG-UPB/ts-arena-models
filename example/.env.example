# Docker Compose project name
COMPOSE_PROJECT_NAME=ts-models

# Port for the Master Controller API
MASTER_API_PORT=8456

# GPU device ID (for GPU setup only)
# Use '0' for first GPU, or 'all' for all GPUs
GPU_ID=0
