# Pinned deliberately to the TabPFN **v2** generation (2026-08-19).
#
# Two independent boundaries make the upper versions unusable here:
#   * tabpfn-time-series 1.1.0 changed the LOCAL-mode default checkpoint to
#     tabpfn-v3-regressor-...-timeseries.ckpt. v3 is a DIFFERENT MODEL, not a
#     bugfix -- adopting it silently would break comparability of every
#     historical tabpfn-ts score on the leaderboard. 1.0.10 is the last v2 default.
#   * tabpfn 7.1.1 added a licence-acceptance gate requiring a TABPFN_TOKEN to
#     download weights, which cannot work on an unattended host. 7.0.0 is the
#     last version without it.
#
# Moving to v3 is a deliberate benchmark decision (new model entry, not an
# in-place upgrade), not a dependency bump.
fastapi
uvicorn
pydantic
pandas
python-dateutil
tabpfn-time-series==1.0.10
tabpfn==7.0.0
torch
numpy
