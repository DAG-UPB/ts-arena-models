# Pinned 2026-08-19 to the versions running in the image that produced the
# clean rounds 11974-11977. Floating specifiers let a cold rebuild jump major
# versions and silently change model behaviour -- that is how chronos-2 and
# tabpfn-ts broke (models #21). Bump deliberately, then rebuild and test.
# torch/torchvision are NOT pinned here on purpose: they ship preinstalled in the
# pytorch/pytorch base image, which the FROM tag already pins exactly. Pinning the
# +cuXXX local version here is unresolvable from PyPI and breaks the build.

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
transformers==4.40.1
granite-tsfm==0.3.1
scipy==1.15.3
numpy==1.26.0

# Transitive numeric/ML stack, pinned explicitly: undeclared here before,
# which is exactly how the chronos-2 break slipped in.
pandas==2.3.3
scikit-learn==1.7.2
huggingface-hub==0.36.2
safetensors==0.8.0
accelerate==1.14.0
datasets==5.0.1
